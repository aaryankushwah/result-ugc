import { CheckCircle2, Clock3, MinusCircle, RefreshCw, WifiOff } from "lucide-react";
import type { TrackingState } from "@result/domain";

export function formatNumber(value: number): string { return new Intl.NumberFormat("en", { notation: value >= 10_000 ? "compact" : "standard", maximumFractionDigits: 1 }).format(value); }
export function formatPercent(value: number): string { return new Intl.NumberFormat("en", { style: "percent", maximumFractionDigits: 1 }).format(value); }
export function formatDate(value: string | null, withTime = false): string { if (!value) return "—"; const date = new Date(value); return new Intl.DateTimeFormat("en", withTime ? { month: "short", day: "numeric", hour: "numeric", minute: "2-digit" } : { month: "short", day: "numeric", year: date.getFullYear() === new Date().getFullYear() ? undefined : "numeric" }).format(date); }
export function timeAgo(value: string | null): string { if (!value) return "Never"; const seconds = Math.floor((Date.now() - new Date(value).getTime()) / 1000); if (seconds < 60) return "Just now"; if (seconds < 3600) return `${Math.floor(seconds / 60)}m ago`; if (seconds < 86_400) return `${Math.floor(seconds / 3600)}h ago`; return `${Math.floor(seconds / 86_400)}d ago`; }
export function TrackingBadge({ state }: { state: TrackingState }) { const config = { healthy: { label: "Healthy", icon: CheckCircle2 }, stale: { label: "Stale", icon: Clock3 }, failed: { label: "Failed", icon: WifiOff }, pending: { label: "Pending", icon: RefreshCw }, untracked: { label: "Untracked", icon: MinusCircle } }[state]; return <span className={`status-badge status-${state}`}><config.icon />{config.label}</span>; }
export function StateBadge({ label, tone = "neutral" }: { label: string; tone?: "neutral" | "success" | "attention" | "info" }) { return <span className={`state-badge tone-${tone}`}>{label.replaceAll("_", " ")}</span>; }
export function PageTitle({ eyebrow, title, description, actions }: { eyebrow?: string; title: string; description?: string; actions?: React.ReactNode }) { return <div className="page-title"><div>{eyebrow ? <p className="eyebrow">{eyebrow}</p> : null}<h1>{title}</h1>{description ? <p>{description}</p> : null}</div>{actions ? <div className="page-actions">{actions}</div> : null}</div>; }
