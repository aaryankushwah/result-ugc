import { KeyRound, Shield, SlidersHorizontal } from "lucide-react";
import { PageTitle } from "@/components/ui";
import { requireUser } from "@/lib/auth";

export default async function SettingsPage() { const user = await requireUser(); return <div className="page-stack"><PageTitle eyebrow="ORGANIZATION" title="Settings" description="Result workspace identity, access policy, and operating defaults." /><section className="settings-grid"><article className="panel"><div className="settings-icon"><Shield /></div><h2>Discord role access</h2><p>Only members of the configured Result guild with an authorized team role can sign in.</p><dl className="settings-list"><div><dt>Your role</dt><dd>{user.role.replace("_", " ")}</dd></div><div><dt>Session length</dt><dd>12 hours</dd></div><div><dt>Production fallback</dt><dd>Fail closed</dd></div></dl></article><article className="panel"><div className="settings-icon"><SlidersHorizontal /></div><h2>Creator operations</h2><p>Defaults preserve provider truth and make queued Discord work explicit.</p><dl className="settings-list"><div><dt>Signing source</dt><dd>Launchpoint API</dd></div><div><dt>SideShift</dt><dd>Manual verification</dd></div><div><dt>Social refresh</dt><dd>15 minutes</dd></div></dl></article><article className="panel"><div className="settings-icon"><KeyRound /></div><h2>Secrets</h2><p>API keys and Discord credentials remain server-side and are never rendered in the portal.</p><dl className="settings-list"><div><dt>Viral API</dt><dd>Configured</dd></div><div><dt>Database</dt><dd>{process.env.DATABASE_URL ? "Configured" : "Pending"}</dd></div><div><dt>Discord OAuth</dt><dd>{process.env.DISCORD_CLIENT_ID ? "Configured" : "Pending"}</dd></div></dl></article></section></div>; }
